
 git 저장소 (repository)         


		 pc      -    local repo  

 	            github  -   remote repo


  https://hoazzinews/tistory.com/63  or 65

  stage에  올려놓고 명령어를 통해 관리

   ->add                ->   commit


 git add . (. 일명 점은 현재 저렇게 입력하면 stage에 모든 파일을 한번에 올림)

상향키 -> 기존의 명령어 목록 뜸. 

reflog (이전 과정 불러오기)

빠져나올 때는  q or ctrl +c


$git(프로그램) config(명령) --global (옵션)   -> 여기서 global은 (모든 영역에서 설정)   

   ex.  $photoshop newfile --name "temp.psd" 


되돌리기 restore  stage 상태의 문서를 다시 수정할 필요가 있을 때 

[참고] 스테이징에 올라간 파일 되돌리기
$ git restore --staged doc.txt


head, master  .  head -> master  차이점  (명령어 git checkout master)

head 와 master 별개로   -> master에 타기 위해서 명령어  git checkout master 입력해서 올라타기 

'git reset --hard'  파일을 완전히 삭제하면서 되돌리기-> head master 분리되지 않고 올라탐


 git remote add origin  -> 로컬에 있는 git을 온라인 저장소(origin)로   remote server
 
 git remote -v (version)  -> 일종의 verification

 git branch -M main   master -> main 으로 변경

clone을 할 때는 init을 통해 저장소를 이중화하지 않는다 (로컬 저장소x)
 
 . (현재  current )

1. 소통 : slack
2. 형상관리(버전관리) :  git   
3. 문서 : notion


git 63, 65, 66, 67, 71, 72 (티스토리 카페)

https://hoazzinews.tistory.com/



vscode 단축키  ctrl +b


https://hoazzinews.tistory.com/
 파이썬 10/11/12


  git init (해당 디렉토리에서 시작)

  git config --global user.email "메일 주소"
  git config --global user.name "계정명"

  git config --list (확인)

  git add . 
  git commit -m "올리고 싶은 명칭"
  git remote add origin "git 저장소 주소"
  git branch -M main  git은 master가 아니라  main 이라서 맞춤
  git push origin main -> 여기까지 해야 파일 올라감


 
  


  

★ 파이썬

 저급언어(컴퓨터 친화적) vs 고급언어(인간 친화적)

고급언어로 작성된 소스코드를 실행하면 코드는 이진수로 이루어진 목적코드(object code)로 변환

소스코드 -> 컴파일러, 인터프리터(ex, 파이썬) -> 목적코드(이진수)
 

    ＊컴파일러(Compiler) - 끝까지 다 읽고 전체 번역  (빠름) - 통번역 느낌

    ＊인터프리터(Interpreter) - 한 줄씩 번역 (느림)  - 동시통역 느낌
 


ERP(Enterprised Resource Planning)

기업의 전사적 자원 관리


파이썬 고유한 특징 (산업 과학분야에 탁월)

-> 무한 정수를 처리가능


 ★ 데이터와 변수

 데이터 - 어떤 물체나 현상을 측정하거나 관찰하여 얻어진 값, 정보를 이루고 있는 구성요소

 정보 - 데이터에 의미 부여 

 어떤 데이터는 임의 메모리 공간으로 들어가게 되는데 그 데이터가 들어간 공간을 변수(varieble)라고 하며 

 그 변수가 있는 공간을 이름을 붙히는걸 변수명
		
	 (C의 경우 메모리관리를 직접하기 때문에 임의의 공간이 아닌 지정된 공간에)


  ex_ )  num1(변수명) = 10(데이터)  ( ' = ' 할당연산자, 대입연산자)


★ 변수 선언과 초기화

 25. Number1 = 100   

 > 메모리 방잡고 명찰달고 청소 ->변수 선언  

 > 특정데이터를 넣는 단계 (초기화)

 	이 모든 것을 ＊변수 정의 단계 (Definition, Def)


  ☆ 할당 연산자 '='는 우측의 데이터를 좌측의 변수에 대입하라는 뜻

 
   ＊dir - 디렉토리 확인

   ＊cd(change directory)  -  디렉토리 변경시 파일명의 일부분을 치고  tab키를 누르면 자동완성됨

      유의사항(d가 안붙어있으면 파일임)

   ＊ python variable.py (terminal에서 파이선 파일 실행)

    ''  empty character > 빈 문자
  
    ' '  -> 데이터가 있음, 공백 문자(열) 

 ★유용한 단축키

🔹 디버깅 없이 실행 (Ctrl + F5)

👉 그냥 처음부터 끝까지 한 번에 실행

중간에 안 멈춤
변수 값 확인 못함
빠름
결과만 확인

👉 쉽게 말하면
“완성된 프로그램처럼 그냥 실행”

🔹 디버깅 실행 (F5)

👉 코드 실행하면서 중간중간 멈추고 분석 가능

예를 들어 Visual Studio Code에서:

브레이크포인트(빨간 점) 찍어두면 거기서 멈춤
변수 값 실시간 확인 가능
한 줄씩 실행 가능 (Step Over, Step Into)
함수 내부까지 들어가서 흐름 추적 가능

👉 쉽게 말하면
“코드를 슬로우 모션으로 보면서 검사하는 모드”

디버깅(Debugging)은 한마디로
👉 프로그램에서 “왜 이상하게 동작하는지” 원인을 찾아내고 고치는 과정이야.


▶ 한 줄 복사 / 이동

Shift + Alt + ↓ → 줄 복사

Alt + ↑ / ↓ → 줄 이동

  일반적인 유의사항

 1. 일반적으로 변수명은 소문자로 시작 -> class  대문자 변수는 소문자
  
 2. 변수명은 해당변수에 어떤 데이터가 저장되는지 쉽게 알 수 있게 함

 3. 2개 이상의 단어가 조합될 경우 낙타표기법에 따른다 (currentLocationLatitude)
    (언더바를 활용하기도 함 -> 스네이크 표기법 ex, current_location_latitude)

 4. 예약어는 변수명으로 사용할 수 없다. 
                
 EX. True, False, None, If,...

                 < list, str, int (최다 사고 구간) >
                -> 정확히는 “예약어”라기보다 내장 함수/타입 이름
   
         자료형 이름 : sum, max, min   or 


  🔹 핵심 차이 (중요)

 종류	 예시	       특징
예약어	if, for	아예 사용 불가 (에러 바로 남)
내장함수	list, sum	사용은 되지만 나중에 터짐 (더 위험)


 5. 언더바( _ )를 제외한 특수문자는 사용  x

 6. 공백 문자  x

 7. 숫자는 첫 글자를 제외한 나머지 자리에서만 사용

 ※ 요약

  영문자로 시작, 소문자로 시작, 데이터의 의미를 쉽게 파악할 수 있게 짓는다(******)
  두 개 이상의 단어가 조합될 경우 낙타표기법을 따른다(*****)
  예약어는 변수명으로 사용할 수 없다 
  언더바를 제외한 특수문자는 사용할 수 없다
  공백 문자는 사용할 수 없다
  숫자는 첫 글자를 제외한 나머지 자리에서만 사용한다. 


 자료형 = 데이터 타입(date type)

 작은 방 큰 데이터 -> 오버플로우(overflow)
 
 큰 방 적은 데이터 -> 메모리 낭비    

 따라서 정수형(integer)은 4바이트 실수형은 8바이트 이런식으로 배치

  java 의 경우 int num1 = 10;   방 배정을 정확하게 해야함 

 파이썬은 방을 늘리고 줄이고 알아서 함  num1 = 10(정수) 을 하든 num1 = 3.14(실수)를 하든 알아서

 flag #boolean 불리언 : True or False   flag 사용 시 True  False  반드시 대문자(파이썬에서만)

 str string 문자열 

 C, C++, Java 작은 큰 따옴표 구분함 <-> 파이썬은 구분 x 하지만 통일 시켜야함

 정수는 메모리 크기에 제약  x

 실수는 메모리 제약  o

 3 = 3.0 같은 값이지만 3.0은 실수임(8 bytes) 

 '#' 주석처리 실행 안 됨   (ctrl + /)   ''' 앞뒤로 '''  이렇게 지정해도 됨

 메모나 특정 부분을 스킵하고 싶을 때 사용

 
 정보처리기사 과목
 SQLD 자격증

 데이터재활용

 데이터복사  var1 = 123 , var2 = var1  다른 메모리 공간에 있지만 데이터만 복사하는 것임

 따라서 이후에 var1 값을 다른 값 321 로 변경했다 하더라도 var2는 여전히 123 값을 유지한다 

 데이터 스와핑 temp  임시로 데이터를 이전시킨 다음 복사를 통해 값을 체인지 한다 

 
 

 
 

   